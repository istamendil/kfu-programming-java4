Реализовать сервис сообщений и переписать систему так, чтобы она его использовала: в контроллере можно было обратиться к сервису и вывести полученное от него сообщение:

  public String index(){
    return messageService.getHello();
  }
